import RestaurantHomeClient from "./RestaurantHomeClient";

export const metadata = {
  title: "Restaurant Partner & Admin Hub — Pet Protocols",
  description: "Official restaurant partner portal, kitchen management, and Superadmin announcements.",
};

export default function RestaurantHomePage() {
  return <RestaurantHomeClient />;
}
