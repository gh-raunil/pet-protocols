import StandardsClient from "./StandardsClient";

export const metadata = {
  title: "Kitchen & Hygiene Standards — Pet Protocols Partner Hub",
  description: "Strict hygiene, packaging, quality control, and temperature protocols for partner kitchens.",
};

export default function StandardsPage() {
  return <StandardsClient />;
}
